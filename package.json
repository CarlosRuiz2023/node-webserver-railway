{
  "name": "06-webserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "npm run build:html && npm run build:css && npm run build:js",
    "build:html": "cp src/*.html build/",
    "build:css": "cp src/*.css build/", 
    "build:js": "cp src/*.js build/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "hbs": "^4.2.0"
  }
}
